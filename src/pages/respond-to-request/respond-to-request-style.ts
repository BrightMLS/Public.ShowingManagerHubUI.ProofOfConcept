import { Theme, makeStyles } from '@material-ui/core/styles'

const RespondToRequestStyle = makeStyles((theme: Theme) => ({
  root: { display: 'flex', justifyContent: 'center' },
}))

export default RespondToRequestStyle
